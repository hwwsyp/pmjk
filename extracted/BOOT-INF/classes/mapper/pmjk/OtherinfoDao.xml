<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tpfh.fintech.modules.pmjk.otherinfo.dao.OtherinfoDao">

    <!--表所有的列名集合-->
    <sql id="allColumns">
            id,
            basicinfo,
            bottomprojectinfo,
            repaymentsource,
            repaymentmethod,
            capitalpurpose,
            incomedistributioncluase,
            coreterms,
            otherimportterms,
            othersubscribers,
            remark,
            createtimestamp,
            updatetimestamp,
            islock,
            versionnum,
            productid    </sql>

    <!--除去主键id的所有列名集合-->
    <sql id="exceptIdColumns">
                basicinfo,
                bottomprojectinfo,
                repaymentsource,
                repaymentmethod,
                capitalpurpose,
                incomedistributioncluase,
                coreterms,
                otherimportterms,
                othersubscribers,
                remark,
                createtimestamp,
                updatetimestamp,
                islock,
                versionnum,
                productid    </sql>
    
    <!-- 查询分页信息 -->
    <select id="getOtherinfoListForPage" resultType="com.tpfh.fintech.modules.pmjk.otherinfo.entity.OtherinfoEntity">
		 select <include refid="allColumns"/> from pmjk_otherinfo
		<where>
          	<if test="id != null and id != ''"> and id=#{id}</if>
          	<if test="basicinfo != null and basicinfo != ''"> and basicinfo=#{basicinfo}</if>
          	<if test="bottomprojectinfo != null and bottomprojectinfo != ''"> and bottomprojectinfo=#{bottomprojectinfo}</if>
          	<if test="repaymentsource != null and repaymentsource != ''"> and repaymentsource=#{repaymentsource}</if>
          	<if test="repaymentmethod != null and repaymentmethod != ''"> and repaymentmethod=#{repaymentmethod}</if>
          	<if test="capitalpurpose != null and capitalpurpose != ''"> and capitalpurpose=#{capitalpurpose}</if>
          	<if test="incomedistributioncluase != null and incomedistributioncluase != ''"> and incomedistributioncluase=#{incomedistributioncluase}</if>
          	<if test="coreterms != null and coreterms != ''"> and coreterms=#{coreterms}</if>
          	<if test="otherimportterms != null and otherimportterms != ''"> and otherimportterms=#{otherimportterms}</if>
          	<if test="othersubscribers != null and othersubscribers != ''"> and othersubscribers=#{othersubscribers}</if>
          	<if test="remark != null and remark != ''"> and remark=#{remark}</if>
          	<if test="createtimestamp != null and createtimestamp != ''"> and createtimestamp=#{createtimestamp}</if>
          	<if test="updatetimestamp != null and updatetimestamp != ''"> and updatetimestamp=#{updatetimestamp}</if>
          	<if test="islock != null and islock != ''"> and islock=#{islock}</if>
          	<if test="versionnum != null and versionnum != ''"> and versionnum=#{versionnum}</if>
          	<if test="productid != null and productid != ''"> and productid=#{productid}</if>
		</where>
	</select>

    <!--查询符合条件的信息列表-->
    <select id="getOtherinfoList" resultType="com.tpfh.fintech.modules.pmjk.otherinfo.entity.OtherinfoEntity">
        select <include refid="allColumns"/> from pmjk_otherinfo
        <where>
            <if test="id != null and id != ''"> and id=#{id}</if>
            <if test="basicinfo != null and basicinfo != ''"> and basicinfo=#{basicinfo}</if>
            <if test="bottomprojectinfo != null and bottomprojectinfo != ''"> and bottomprojectinfo=#{bottomprojectinfo}</if>
            <if test="repaymentsource != null and repaymentsource != ''"> and repaymentsource=#{repaymentsource}</if>
            <if test="repaymentmethod != null and repaymentmethod != ''"> and repaymentmethod=#{repaymentmethod}</if>
            <if test="capitalpurpose != null and capitalpurpose != ''"> and capitalpurpose=#{capitalpurpose}</if>
            <if test="incomedistributioncluase != null and incomedistributioncluase != ''"> and incomedistributioncluase=#{incomedistributioncluase}</if>
            <if test="coreterms != null and coreterms != ''"> and coreterms=#{coreterms}</if>
            <if test="otherimportterms != null and otherimportterms != ''"> and otherimportterms=#{otherimportterms}</if>
            <if test="othersubscribers != null and othersubscribers != ''"> and othersubscribers=#{othersubscribers}</if>
            <if test="remark != null and remark != ''"> and remark=#{remark}</if>
            <if test="createtimestamp != null and createtimestamp != ''"> and createtimestamp=#{createtimestamp}</if>
            <if test="updatetimestamp != null and updatetimestamp != ''"> and updatetimestamp=#{updatetimestamp}</if>
            <if test="islock != null and islock != ''"> and islock=#{islock}</if>
            <if test="versionnum != null and versionnum != ''"> and versionnum=#{versionnum}</if>
            <if test="productid != null and productid != ''"> and productid=#{productid}</if>
        </where>
    </select>

	<!-- 新增实体信息 -->
    <insert id="addOtherinfo" useGeneratedKeys="true" keyProperty="id" parameterType="java.util.HashMap">
        insert into pmjk_otherinfo (<include refid="exceptIdColumns"/>)
        values(
                #{basicinfo},
                #{bottomprojectinfo},
                #{repaymentsource},
                #{repaymentmethod},
                #{capitalpurpose},
                #{incomedistributioncluase},
                #{coreterms},
                #{otherimportterms},
                #{othersubscribers},
                #{remark},
                #{createtimestamp},
                #{updatetimestamp},
                #{islock},
                #{versionnum},
                #{productid}        );
    </insert>

    
</mapper>
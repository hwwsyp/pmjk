<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tpfh.fintech.modules.pmjk.institutionsrela.dao.InstitutionsrelaDao">

    <!--表所有的列名集合-->
    <sql id="allColumns">
            id,
            danbaorentype,
            institutionsid,
            
            internalratinglevel,
            internalratinguser,
            internalratingdate,
            externalratingdesc,
            createtimestamp,
            updatetimestamp,
            islock,
            creditenhanceid    </sql>

    <!--除去主键id的所有列名集合-->
    <sql id="exceptIdColumns">
                danbaorentype,
                institutionsid,
                
                internalratinglevel,
                internalratinguser,
                internalratingdate,
                externalratingdesc,
                createtimestamp,
                updatetimestamp,
                islock,
                creditenhanceid    </sql>
    
    <!-- 查询分页信息 -->
    <select id="getInstitutionsrelaListForPage" resultType="com.tpfh.fintech.modules.pmjk.institutionsrela.entity.InstitutionsrelaEntity">
		 select <include refid="allColumns"/> from pmjk_institutionsrela
		<where>
          	<if test="id != null and id != ''"> and id=#{id}</if>
          	<if test="danbaorentype != null and danbaorentype != ''"> and danbaorentype=#{danbaorentype}</if>
          	<if test="institutionsid != null and institutionsid != ''"> and institutionsid=#{institutionsid}</if>
          	 
          	<if test="internalratinglevel != null and internalratinglevel != ''"> and internalratinglevel=#{internalratinglevel}</if>
          	<if test="internalratinguser != null and internalratinguser != ''"> and internalratinguser=#{internalratinguser}</if>
          	<if test="internalratingdate != null and internalratingdate != ''"> and internalratingdate=#{internalratingdate}</if>
          	<if test="externalratingdesc != null and externalratingdesc != ''"> and externalratingdesc=#{externalratingdesc}</if>
          	<if test="createtimestamp != null and createtimestamp != ''"> and createtimestamp=#{createtimestamp}</if>
          	<if test="updatetimestamp != null and updatetimestamp != ''"> and updatetimestamp=#{updatetimestamp}</if>
          	<if test="islock != null and islock != ''"> and islock=#{islock}</if>
          	<if test="creditenhanceid != null and creditenhanceid != ''"> and creditenhanceid=#{creditenhanceid}</if>
		</where>
	</select>

    <!--查询符合条件的信息列表-->
    <select id="getInstitutionsrelaList" resultType="com.tpfh.fintech.modules.pmjk.institutionsrela.entity.InstitutionsrelaEntity">
        select <include refid="allColumns"/> from pmjk_institutionsrela
        <where>
            <if test="id != null and id != ''"> and id=#{id}</if>
            <if test="danbaorentype != null and danbaorentype != ''"> and danbaorentype=#{danbaorentype}</if>
            <if test="institutionsid != null and institutionsid != ''"> and institutionsid=#{institutionsid}</if>
             
            <if test="internalratinglevel != null and internalratinglevel != ''"> and internalratinglevel=#{internalratinglevel}</if>
            <if test="internalratinguser != null and internalratinguser != ''"> and internalratinguser=#{internalratinguser}</if>
            <if test="internalratingdate != null and internalratingdate != ''"> and internalratingdate=#{internalratingdate}</if>
            <if test="externalratingdesc != null and externalratingdesc != ''"> and externalratingdesc=#{externalratingdesc}</if>
            <if test="createtimestamp != null and createtimestamp != ''"> and createtimestamp=#{createtimestamp}</if>
            <if test="updatetimestamp != null and updatetimestamp != ''"> and updatetimestamp=#{updatetimestamp}</if>
            <if test="islock != null and islock != ''"> and islock=#{islock}</if>
            <if test="creditenhanceid != null and creditenhanceid != ''"> and creditenhanceid=#{creditenhanceid}</if>
        </where>
    </select>

	<!-- 新增实体信息 -->
    <insert id="addInstitutionsrela" useGeneratedKeys="true" keyProperty="id" parameterType="java.util.HashMap">
        insert into pmjk_institutionsrela (<include refid="exceptIdColumns"/>)
        values(
                #{danbaorentype},
                #{institutionsid},
                 
                #{internalratinglevel},
                #{internalratinguser},
                #{internalratingdate},
                #{externalratingdesc},
                #{createtimestamp},
                #{updatetimestamp},
                #{islock},
                #{creditenhanceid}        );
    </insert>

    
</mapper>